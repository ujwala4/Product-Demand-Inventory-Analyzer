[project]
name = "product-demand-inventory-analyzer"
version = "0.1.0"
description = "Product Demand Inventory Analyzer"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "gradio==5.49.1",
    "pandas",
    "requests",
    "uvicorn",
    "pydantic",
]
